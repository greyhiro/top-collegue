import { SearchByPseudoPipe } from './search-by-pseudo.pipe';

describe('SearchByPseudoPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByPseudoPipe();
    expect(pipe).toBeTruthy();
  });
});
