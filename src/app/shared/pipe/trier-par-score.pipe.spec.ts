import { TrierParScorePipe } from './trier-par-score.pipe';

describe('TrierParScorePipe', () => {
  it('create an instance', () => {
    const pipe = new TrierParScorePipe();
    expect(pipe).toBeTruthy();
  });
});
